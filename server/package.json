{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"mongod\" \"stack build && stack exec listeo-exe\"",
	"build": "stack build"
  },
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
